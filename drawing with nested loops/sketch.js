// drawing with nested loops
// melsh.capalot
// somehwere in ocotber



function setup() {
  createCanvas(windowWidth, windowHeight);
  loopReview();
}
function loopReview(){
  for(let x = 0; x <=40; x = x+20 ){
    for(let y = 0; y )
  }
}

function draw() {
  background(220);
  renderGrid();
}
function rendergrid
