// Cordinate systems
// melsh.capalot
// 09/11/24
//Looking at how coordinares work in p5.js



function setup() {
  print("Setup runs once,at the star.")
  createCanvas(500, 400);
}

function draw(){
//runs over and over (targeting 60fp)
//when possibles, keep draw() short
background (220);
drawCircles();



}
function drawCircles() {
  //draw some (5) circles
  fill(100,200,205);  //R, G, B
  circle(0,0,50); 

  fill(220,102,0);
  circle(100,-20,50);

  circle (500,)
}
