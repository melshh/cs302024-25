// racer
//melsh.caplot
// somewher in october



let racer1, racer2, racer3;

class RoundRacer {
    constructor(yPosition, color) {
        this.xPosition = 0;
        this.yPosition = yPosition;
        this.xSpeed = Math.floor(Math.random() * (15 - 3 + 1)) + 3; // Random speed between 3 and 15
        this.color = color;
    }

 
}