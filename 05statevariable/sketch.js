// State variables
// melsh.capalot
// Date
//global variables
let currentColor = 0; //0,1 , 2
let circleSize = 50;
let myColor;


function setup() {
  createCanvas(windowWidth, windowHeight);
  myColor = color(50, 230, 165)
}

function draw() {
  background(myColor);

}

//circle code - fill value using SWITCH statement
switch(currentColor){ // what color is the value of current?
  case 0:
fill(255,0,0)
case 1

}
